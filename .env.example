# 100% Zero-API-Key Platform Configuration
# No API keys are required for summarization, document extraction, chat, or OCR.
# Optional Gemini API key for server-assisted inference
GEMINI_API_KEY=
# Optional SMTP configuration for outgoing emails (No API keys required)
SMTP_FROM_NAME=
SMTP_HOST=
SMTP_PASS=
SMTP_PORT=
SMTP_USER=